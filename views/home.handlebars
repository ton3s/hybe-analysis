<!DOCTYPE html>
<html>
<head>
    <title>Trial {{trialNumber}}</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>
<body>
    <h1 class="main-title">Trial {{trialNumber}}</h1>
    <form action="/" method="GET">
		<select name="trial" onchange="this.form.submit()">
			{{#each trialNumbers}}
			<option value="{{this}}" {{#if (eq ../trialNumber this)}}selected{{/if}}>Trial {{this}}</option>
			{{/each}}
		</select>
	</form>
    <div class="container">
        <div class="timestamp">
			<p>Created At: {{data.created_at}}</p>
			<p>Updated At: {{data.updated_at}}</p>
		</div>
        {{#each data.content}}
            <div>
                <h3>Role: {{this.role}}</h3>
                <pre>{{this.content}}</pre>
            </div>
        {{/each}}
    </div>
</body>
</html>
